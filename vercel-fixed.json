{
  "buildCommand": "CI=false TSC_COMPILE_ON_ERROR=true react-scripts build",
  "outputDirectory": "build",
  "devCommand": "react-scripts start",
  "installCommand": "npm install --legacy-peer-deps",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
